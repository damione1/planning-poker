# Planning Poker Environment Configuration

# Development Mode
# Set to "true" to disable Secure cookie flag for localhost development
# In production, this should be "false" or unset (requires HTTPS)
DEV_MODE=true

# WebSocket Allowed Origins
# Comma-separated list of allowed WebSocket origin patterns
# Development default: localhost:*,127.0.0.1:*,host.docker.internal:*
# Production example: yourdomain.com,*.yourdomain.com,api.yourdomain.com
WS_ALLOWED_ORIGINS=localhost:*,127.0.0.1:*,host.docker.internal:*

# Timezone (optional)
# Default: UTC
TZ=UTC
